#include <stdio.h>

/**
 * main - Entry point; prints message and returns 0 for success.
 *
 * Return: 0 for success, non-zero for an error.
 */


int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
